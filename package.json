{
  "name": "zeroclick",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "pnpm --filter shared build && pnpm --parallel --filter \"!@zeroclick/shared\" dev",
    "build": "pnpm --filter shared build && pnpm --filter client build && pnpm --filter server build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
